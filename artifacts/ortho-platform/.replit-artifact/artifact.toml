kind = "web"
previewPath = "/"
title = "OrthoVision Platform"
version = "1.0.0"
id = "artifacts/ortho-platform"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21567

[services.development]
run = "pnpm --filter @workspace/ortho-platform run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ortho-platform", "run", "build" ]
publicDir = "artifacts/ortho-platform/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21567"
BASE_PATH = "/"
